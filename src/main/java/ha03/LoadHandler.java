package ha03;

import java.util.Stack;

public class LoadHandler implements CommandHandler {
    @Override
    public boolean command(String command, Stack variableStack){
        System.out.println("LoadHandler loads " + );
    }
}
